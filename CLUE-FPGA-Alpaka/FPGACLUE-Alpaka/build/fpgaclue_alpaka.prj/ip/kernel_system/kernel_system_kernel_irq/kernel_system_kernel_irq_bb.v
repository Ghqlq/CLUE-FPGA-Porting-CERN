module kernel_system_kernel_irq (
		input  wire       clk,          //          clk.clk
		input  wire [0:0] receiver_irq, // receiver_irq.irq
		input  wire       reset,        //    clk_reset.reset
		output wire       sender0_irq   //  sender0_irq.irq
	);
endmodule

