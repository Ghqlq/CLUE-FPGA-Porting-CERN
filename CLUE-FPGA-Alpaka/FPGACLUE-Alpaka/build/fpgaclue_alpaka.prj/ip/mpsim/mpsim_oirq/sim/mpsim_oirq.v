// mpsim_oirq.v

// Generated using ACDS version 25.1.1 125

`timescale 1 ps / 1 ps
module mpsim_oirq (
		input  wire  clock,            //            clock.clk
		input  wire  resetn,           //            reset.reset_n
		input  wire  clock2x,          //          clock2x.clk
		output wire  trigger_reset,    //       reset_ctrl.conduit
		input  wire  kernel_interrupt  // kernel_interrupt.irq
	);

	aoc_sim_main_dpi_controller oirq (
		.clock            (clock),            //   input,  width = 1,            clock.clk
		.resetn           (resetn),           //   input,  width = 1,            reset.reset_n
		.clock2x          (clock2x),          //   input,  width = 1,          clock2x.clk
		.trigger_reset    (trigger_reset),    //  output,  width = 1,       reset_ctrl.conduit
		.kernel_interrupt (kernel_interrupt)  //   input,  width = 1, kernel_interrupt.irq
	);

endmodule
